<%= form_with(model: chop) do |form| %>
  <% if chop.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(chop.errors.count, "error") %> prohibited this chop from being saved:</h2>

      <ul>
        <% chop.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :solve, style: "display: block" %>
    <%= form.text_area :solve %>
  </div>

  <div>
    <%= form.label :solution, style: "display: block" %>
    <%= form.text_area :solution %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
